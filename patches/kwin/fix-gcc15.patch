--- a/src/core/renderdevice.cpp
+++ b/src/core/renderdevice.cpp
@@ -179,15 +179,15 @@
-    auto [result, instance] = context.createInstance(instanceInfo);
-    if (result != vk::Result::eSuccess && !validationLayers.empty()) {
+    auto expectedInstance = context.createInstance(instanceInfo);
+    if (expectedInstance.error() != vk::Result::eSuccess && !validationLayers.empty()) {
         // try again without the validation layer
         validationLayers.clear();
         instanceInfo.setPEnabledLayerNames(validationLayers);
-        auto [result, instance] = context.createInstance(instanceInfo);
-        if (result == vk::Result::eSuccess) {
+        expectedInstance = context.createInstance(instanceInfo);
+        if (expectedInstance.error() == vk::Result::eSuccess) {
             qCWarning(KWIN_CORE, "Vulkan validation layer is not installed");
-            return std::move(instance);
+            return std::move(expectedInstance.value());
         }
     }
-    return std::move(instance);
+    return std::move(expectedInstance.value());
 }
 
 static std::unique_ptr<VulkanDevice> openVulkanDevice(const vk::raii::Instance &instance, DrmDevice *drm)
 {
-    const auto [enumerateResult, physicalDevices] = instance.enumeratePhysicalDevices();
-    if (enumerateResult != vk::Result::eSuccess) {
-        qCWarning(KWIN_VULKAN) << "querying vulkan devices failed:" << vk::to_string(enumerateResult);
+    auto expectedPhysicalDevices = instance.enumeratePhysicalDevices();
+    if (expectedPhysicalDevices.error() != vk::Result::eSuccess) {
+        qCWarning(KWIN_VULKAN) << "querying vulkan devices failed:" << vk::to_string(expectedPhysicalDevices.error());
         return nullptr;
     }
-    const auto &physicalDevices = physicalDevices; 
+    const auto &physicalDevices = expectedPhysicalDevices.value();
 
     // ...
 
-        const auto [extensionPropResult, extensionProps] = physicalDevice.enumerateDeviceExtensionProperties();
-        if (extensionPropResult != vk::Result::eSuccess) {
-            continue;
-        }
+        const auto extensionProps = physicalDevice.enumerateDeviceExtensionProperties();
         std::vector usedExtensions = s_requiredVulkanExtensions | std::ranges::to<std::vector>();
         if (isSoftwareDevice) {
             // software devices don't usually have a render node
             std::erase(usedExtensions, VK_EXT_PHYSICAL_DEVICE_DRM_EXTENSION_NAME);
         }
         std::vector missingExtensions = usedExtensions;
-        std::erase_if(missingExtensions, [&extensionProps](std::string_view required) {
-            return std::ranges::any_of(extensionProps, [required](const auto &ext) {
-                return required == ext.extensionName;
-            });
-        });
+        for (auto it = missingExtensions.begin(); it != missingExtensions.end();) {
+            const auto &required = *it;
+            const bool found = std::ranges::any_of(extensionProps, [required](const auto &ext) {
+                return std::string_view(required) == std::string_view(ext.extensionName);
+            });
+            if (found) {
+                it = missingExtensions.erase(it);
+            } else {
+                ++it;
+            }
+        }
         if (!missingExtensions.empty()) {
             // ...
         }
         
         // ...
         
-        auto [result, logicalDevice] = physicalDevice.createDevice(deviceInfo);
-        if (result != vk::Result::eSuccess) {
-            qCWarning(KWIN_VULKAN, "vkCreateDevice for %s failed: %s", deviceName, vk::to_string(vk::Result(result)).c_str());
+        auto expectedLogicalDevice = physicalDevice.createDevice(deviceInfo);
+        if (expectedLogicalDevice.error() != vk::Result::eSuccess) {
+            qCWarning(KWIN_VULKAN, "vkCreateDevice for %s failed: %s", deviceName, vk::to_string(vk::Result(expectedLogicalDevice.error())).c_str());
             continue;
         }
-        auto logicalDevice = std::move(logicalDevice);
+        auto logicalDevice = std::move(expectedLogicalDevice.value());
--- a/src/vulkan/vulkan_device.cpp
+++ b/src/vulkan/vulkan_device.cpp
@@ -70,1 +70,1 @@
-    auto [result, cmdPool] = m_logical.createCommandPool(vk::CommandPoolCreateInfo{
+    auto expectedCmdPool = m_logical.createCommandPool(vk::CommandPoolCreateInfo{
@@ -72,3 +72,3 @@
-    if (result != vk::Result::eSuccess) {
-        qCCritical(KWIN_VULKAN) << "creating a command pool failed:" << vk::to_string(result);
+    if (expectedCmdPool.error() != vk::Result::eSuccess) {
+        qCCritical(KWIN_VULKAN) << "creating a command pool failed:" << vk::to_string(expectedCmdPool.error());
         return;
     }
-    m_commandPool = std::move(cmdPool);
+    m_commandPool = std::move(expectedCmdPool.value());
@@ -175,3 +175,3 @@
-    auto [imageResult, image] = m_logical.createImage(imageInfo);
-    if (imageResult != vk::Result::eSuccess) {
-        qCWarning(KWIN_VULKAN) << "creating vulkan image failed!" << vk::to_string(imageResult);
+    auto expectedImage = m_logical.createImage(imageInfo);
+    if (expectedImage.error() != vk::Result::eSuccess) {
+        qCWarning(KWIN_VULKAN) << "creating vulkan image failed!" << vk::to_string(expectedImage.error());
         return nullptr;
     }
-    auto image = std::move(image);
+    auto image = std::move(expectedImage.value());
@@ -193,3 +193,3 @@
-        const auto [memoryFdResult, memoryFdProperties] = m_logical.getMemoryFdPropertiesKHR(vk::ExternalMemoryHandleTypeFlagBits::eDmaBufEXT, duplicatedFds[i].get());
-        if (memoryFdResult != vk::Result::eSuccess) {
-            qCWarning(KWIN_VULKAN) << "failed to get memory fd properties!" << vk::to_string(memoryFdResult);
+        const auto expectedMemoryFdProperties = m_logical.getMemoryFdPropertiesKHR(vk::ExternalMemoryHandleTypeFlagBits::eDmaBufEXT, duplicatedFds[i].get());
+        if (expectedMemoryFdProperties.error() != vk::Result::eSuccess) {
+            qCWarning(KWIN_VULKAN) << "failed to get memory fd properties!" << vk::to_string(expectedMemoryFdProperties.error());
             return nullptr;
         }
-        const auto &memoryFdProperties = memoryFdProperties;
+        const auto &memoryFdProperties = expectedMemoryFdProperties.value();
@@ -229,3 +229,3 @@
-        auto [allocateResult, memory] = m_logical.allocateMemory(memoryInfo);
-        if (allocateResult != vk::Result::eSuccess) {
-            qCWarning(KWIN_VULKAN) << "'Allocating' memory for dmabuf failed: %s", vk::to_string(allocateResult).c_str();
+        auto expectedMemory = m_logical.allocateMemory(memoryInfo);
+        if (expectedMemory.error() != vk::Result::eSuccess) {
+            qCWarning(KWIN_VULKAN) << "'Allocating' memory for dmabuf failed: %s", vk::to_string(expectedMemory.error()).c_str();
             return nullptr;
         }
-        auto memory = std::move(memory);
+        auto memory = std::move(expectedMemory.value());
@@ -391,3 +391,3 @@
-    auto [result, buffers] = m_logical.allocateCommandBuffers(vk::CommandBufferAllocateInfo{
+    auto expectedBuffers = m_logical.allocateCommandBuffers(vk::CommandBufferAllocateInfo{
         m_commandPool,
         vk::CommandBufferLevel::ePrimary,
         1,
     });
-    if (result != vk::Result::eSuccess) {
-        qCWarning(KWIN_VULKAN) << "Failed to create a command buffer" << vk::to_string(result);
+    if (expectedBuffers.error() != vk::Result::eSuccess) {
+        qCWarning(KWIN_VULKAN) << "Failed to create a command buffer" << vk::to_string(expectedBuffers.error());
         return nullptr;
     }
-    auto buffers = std::move(buffers);
+    auto buffers = std::move(expectedBuffers.value());
@@ -409,3 +409,3 @@
-    auto [result, semaphore] = m_logical.createSemaphore(semaphoreInfo);
-    if (result != vk::Result::eSuccess) {
+    auto expectedSemaphore = m_logical.createSemaphore(semaphoreInfo);
+    if (expectedSemaphore.error() != vk::Result::eSuccess) {
         return std::nullopt;
     }
-    auto semaphore = std::move(semaphore);
+    auto semaphore = std::move(expectedSemaphore.value());
@@ -433,3 +433,3 @@
-    auto [fenceResult, fence] = m_logical.createFence(vk::FenceCreateInfo{
+    auto expectedFence = m_logical.createFence(vk::FenceCreateInfo{
         vk::FenceCreateFlags{},
         &exportInfo,
     });
-    if (fenceResult != vk::Result::eSuccess) {
+    if (expectedFence.error() != vk::Result::eSuccess) {
         return std::nullopt;
     }
-    auto fence = std::move(fence);
+    auto fence = std::move(expectedFence.value());
@@ -460,3 +460,3 @@
-    const auto [fdResult, fd] = m_logical.getFenceFdKHR(vk::FenceGetFdInfoKHR{
-        fence,
-        vk::ExternalFenceHandleTypeFlagBits::eSyncFd,
-    });
-    if (fdResult != vk::Result::eSuccess) {
+    const auto expectedFd = m_logical.getFenceFdKHR(vk::FenceGetFdInfoKHR{
+        fence,
+        vk::ExternalFenceHandleTypeFlagBits::eSyncFd,
+    });
+    if (expectedFd.error() != vk::Result::eSuccess) {
         return std::nullopt;
     }
-    const auto fd = fd;
+    const auto fd = expectedFd.value();
@@ -492,3 +492,3 @@
-        auto [result, ret] = m_logical.allocateMemory(vk::MemoryAllocateInfo{
-            requirements.memoryRequirements.size,
-            *typeIndex,
-        });
-        if (result == vk::Result::eSuccess) {
-            return std::move(ret);
-        } else {
-            qCWarning(KWIN_VULKAN) << "Allocating memory for an image failed:" << vk::to_string(result);
+        auto expectedMemory = m_logical.allocateMemory(vk::MemoryAllocateInfo{
+            requirements.memoryRequirements.size,
+            *typeIndex,
+        });
+        if (expectedMemory.error() == vk::Result::eSuccess) {
+            return std::move(expectedMemory.value());
+        } else {
+            qCWarning(KWIN_VULKAN) << "Allocating memory failed:" << vk::to_string(expectedMemory.error());
             return nullptr;
         }
