--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -63,7 +63,9 @@ endif()
 
 if (UNIX AND NOT APPLE AND NOT HAIKU)
-    set(HAVE_X11 TRUE)
+    find_package(X11)
+    if (X11_FOUND)
+        set(HAVE_X11 TRUE)
+    endif()
 endif()
