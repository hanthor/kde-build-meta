diff --git a/CMakeLists.txt b/CMakeLists.txt
index 04c97e0..056f260 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -168,8 +168,6 @@ set(ksld_SRCS
    abstractlocker.cpp
    ksldapp.cpp
    interface.cpp
-   globalaccel.cpp
-   x11locker.cpp
    waylandlocker.cpp
    logind.cpp
    waylandserver.cpp
@@ -177,8 +175,6 @@ set(ksld_SRCS
    abstractlocker.h
    ksldapp.h
    interface.h
-   globalaccel.h
-   x11locker.h
    waylandlocker.h
    logind.h
    waylandserver.h
@@ -205,6 +201,12 @@ ecm_add_wayland_server_protocol(ksld_SRCS
 
 add_library(KScreenLocker SHARED ${ksld_SRCS})
 
+if (X11_FOUND)
+    target_sources(KScreenLocker PRIVATE globalaccel.cpp x11locker.cpp)
+    target_compile_definitions(KScreenLocker PRIVATE HAVE_X11)
+    target_link_libraries(KScreenLocker PRIVATE X11::X11 XCB::XCB XCB::KEYSYMS)
+endif()
+
 target_link_libraries(KScreenLocker
 PUBLIC
    Qt::Core
@@ -219,9 +221,6 @@ PRIVATE
    KF6::ConfigGui
    KF6::Package
    KF6::WindowSystem
-   X11::X11
-   XCB::XCB
-   XCB::KEYSYMS
    Wayland::Server
 )
 
diff --git a/abstractlocker.h b/abstractlocker.h
index 047034c..fc65f9a 100644
--- a/abstractlocker.h
+++ b/abstractlocker.h
@@ -14,7 +14,9 @@ SPDX-License-Identifier: GPL-2.0-or-later
 #include <QObject>
 #include <QRasterWindow>
 
+#ifdef HAVE_X11
 class GlobalAccel;
+#endif
 
 namespace ScreenLocker
 {
@@ -49,10 +51,12 @@ public:
 
     virtual void addAllowedWindow(quint32 window);
 
+#ifdef HAVE_X11
     void setGlobalAccel(GlobalAccel *ga)
     {
         m_globalAccel = ga;
     }
+#endif
 
     void emergencyShow();
 
@@ -63,14 +67,18 @@ Q_SIGNALS:
 protected:
     virtual void stayOnTop() = 0;
 
+#ifdef HAVE_X11
     GlobalAccel *globalAccel()
     {
         return m_globalAccel;
     }
+#endif
     QScopedPointer<BackgroundWindow> m_background;
 
 private:
+#ifdef HAVE_X11
     GlobalAccel *m_globalAccel = nullptr;
+#endif
 
     friend class BackgroundWindow;
 };
diff --git a/greeter/CMakeLists.txt b/greeter/CMakeLists.txt
index 7c31624..79c4315 100644
--- a/greeter/CMakeLists.txt
+++ b/greeter/CMakeLists.txt
@@ -60,11 +60,13 @@ target_link_libraries(kscreenlocker_greet
     Plasma::PlasmaQuick
     Qt::Quick
     Qt::Qml
-    X11::X11
     Wayland::Client
     LayerShellQt::Interface
     KF6::ScreenDpms
 )
+if(X11_FOUND)
+    target_link_libraries(kscreenlocker_greet X11::X11)
+endif()
 
 # KSCREENLOCKER_PAM_SERVICE, if defined, will already have been
 # enclosed in double quotes by the define_pam_service macro.
diff --git a/greeter/greeterapp.cpp b/greeter/greeterapp.cpp
index 004da3e..9e59fa3 100644
--- a/greeter/greeterapp.cpp
+++ b/greeter/greeterapp.cpp
@@ -609,9 +609,11 @@ bool UnlockApp::eventFilter(QObject *obj, QEvent *event)
         }
         if (view && view->winId() && KWindowSystem::isPlatformX11()) {
             // showing greeter view window, set property
+#ifdef HAVE_X11
             auto x11App = qGuiApp->nativeInterface<QNativeInterface::QX11Application>();
             static Atom tag = XInternAtom(x11App->display(), "_KDE_SCREEN_LOCKER", False);
             XChangeProperty(x11App->display(), view->winId(), tag, tag, 32, PropModeReplace, nullptr, 0);
+#endif
         }
         // no further processing
         return false;
diff --git a/ksldapp.cpp b/ksldapp.cpp
index 459ba85..702c31b 100644
--- a/ksldapp.cpp
+++ b/ksldapp.cpp
@@ -7,13 +7,17 @@
 SPDX-License-Identifier: GPL-2.0-or-later
 */
 #include "ksldapp.h"
+#ifdef HAVE_X11
 #include "globalaccel.h"
+#endif
 #include "interface.h"
 #include "kscreensaversettings.h"
 #include "logind.h"
 #include "powermanagement_inhibition.h"
 #include "waylandlocker.h"
+#ifdef HAVE_X11
 #include "x11locker.h"
+#endif
 
 #include "kscreenlocker_logging.h"
 #include <config-kscreenlocker.h>
@@ -35,12 +39,14 @@ SPDX-License-Identifier: GPL-2.0-or-later
 #include <QProcess>
 #include <QTimer>
 // X11
+#ifdef HAVE_X11
 #include "x11info.h"
 #include <X11/Xlib.h>
 #include <xcb/xcb.h>
 #if X11_Xinput_FOUND
 #include <X11/extensions/XInput2.h>
 #endif
+#endif // HAVE_X11
 // other
 #include <signal.h>
 #include <unistd.h>
@@ -116,7 +122,9 @@ void KSldApp::cleanUp()
     delete m_lockWindow;
 
     // Restore X screensaver parameters
+#ifdef HAVE_X11
     XSetScreenSaver(X11Info::display(), s_XTimeout, s_XInterval, s_XBlanking, s_XExposures);
+#endif
 }
 
 static bool s_graceTimeKill = false;
@@ -170,7 +178,9 @@ void KSldApp::initialize()
     m_requirePassword = KScreenSaverSettings::requirePassword();
 
     if (m_isX11) {
+#ifdef HAVE_X11
         initializeX11();
+#endif
     }
 
     // Global keys
@@ -354,9 +364,11 @@ void KSldApp::initialize()
         }
     });
 
+#ifdef HAVE_X11
     m_globalAccel = new GlobalAccel(this);
     connect(this, &KSldApp::locked, m_globalAccel, &GlobalAccel::prepare);
     connect(this, &KSldApp::unlocked, m_globalAccel, &GlobalAccel::release);
+#endif
 
     // fallback for non-logind systems:
     // connect to signal emitted by Solid. This is emitted unconditionally also on logind enabled systems
@@ -492,6 +504,7 @@ bool KSldApp::establishGrab()
     if (!m_isX11) {
         return true;
     }
+#ifdef HAVE_X11
     XSync(X11Info::display(), False);
     XServerGrabber serverGrabber;
     if (!grabKeyboard()) {
@@ -556,11 +569,11 @@ bool KSldApp::establishGrab()
         XFlush(dpy);
         return success;
     }
-#endif
-
+#endif // HAVE_X11
     return true;
 }
 
+#ifdef HAVE_X11
 static bool grabKeyboard()
 {
     qCDebug(KSCREENLOCKER) << "Grabbing keyboard";
@@ -569,7 +582,9 @@ static bool grabKeyboard()
 
     return (rv == GrabSuccess);
 }
+#endif // HAVE_X11
 
+#ifdef HAVE_X11
 static bool grabMouse()
 {
     qCDebug(KSCREENLOCKER) << "Grabbing mouse";
@@ -580,12 +595,14 @@ static bool grabMouse()
 
     return (rv == GrabSuccess);
 }
+#endif // HAVE_X11
 
 void KSldApp::doUnlock()
 {
     qCDebug(KSCREENLOCKER) << "Unlocking now.";
 
     if (m_isX11) {
+#ifdef HAVE_X11
         xcb_connection_t *c = X11Info::connection();
         xcb_ungrab_keyboard(c, XCB_CURRENT_TIME);
         xcb_ungrab_pointer(c, XCB_CURRENT_TIME);
@@ -603,7 +620,7 @@ void KSldApp::doUnlock()
             XIFreeDeviceInfo(masters);
             XFlush(dpy);
         }
-#endif
+#endif // HAVE_X11
     }
     hideLockWindow();
     // delete the window again, to get rid of event filter
@@ -708,6 +725,7 @@ void KSldApp::showLockWindow()
         qCDebug(KSCREENLOCKER) << "Creating lock window";
 
         if (m_isX11) {
+#ifdef HAVE_X11
             m_lockWindow = new X11Locker(this);
 
             connect(
@@ -720,6 +738,7 @@ void KSldApp::showLockWindow()
                     }
                 },
                 Qt::QueuedConnection);
+#endif // HAVE_X11
         }
 
         if (m_isWayland) {
@@ -728,7 +747,9 @@ void KSldApp::showLockWindow()
         if (!m_lockWindow) {
             return;
         }
+#ifdef HAVE_X11
         m_lockWindow->setGlobalAccel(m_globalAccel);
+#endif
 
         connect(m_lockWindow, &AbstractLocker::lockWindowShown, this, &KSldApp::lockScreenShown);
 
@@ -736,7 +757,9 @@ void KSldApp::showLockWindow()
     }
     m_lockWindow->showLockWindow();
     if (m_isX11) {
+#ifdef HAVE_X11
         XSync(X11Info::display(), False);
+#endif
     }
 }
 
@@ -858,11 +881,13 @@ bool KSldApp::event(QEvent *event)
 {
     qCDebug(KSCREENLOCKER) << "Event received";
 
+#ifdef HAVE_X11
     if (event->type() == QEvent::KeyPress && m_globalAccel) {
         if (m_globalAccel->keyEvent(static_cast<QKeyEvent *>(event))) {
             event->setAccepted(true);
         }
     }
+#endif
     return false;
 }
 
diff --git a/ksldapp.h b/ksldapp.h
index bcc711c..8f34c9b 100644
--- a/ksldapp.h
+++ b/ksldapp.h
@@ -14,7 +14,9 @@ SPDX-License-Identifier: GPL-2.0-or-later
 #include <KScreenLocker/kscreenlocker_export.h>
 
 // forward declarations
+#ifdef HAVE_X11
 class GlobalAccel;
+#endif
 class LogindIntegration;
 class QTimer;
 class KSldTest;
@@ -291,10 +293,12 @@ public Q_SLOTS:
     void lockScreenShown();
 
 private:
+#ifdef HAVE_X11
     /**
      * @brief Initializes the X11 environment for the KSldApp.
      */
     void initializeX11();
+#endif
 
     /**
      * @brief Establishes the grab for keyboard and mouse input.
@@ -408,15 +412,19 @@ private:
      */
     LogindIntegration *m_logind;
 
+#ifdef HAVE_X11
     /**
      * @brief Provides handling for global accelerators.
      */
     GlobalAccel *m_globalAccel = nullptr;
+#endif
 
+#ifdef HAVE_X11
     /**
      * @brief Whether the XInput2 extension is available.
      */
     bool m_hasXInput2 = false;
+#endif
 
     /**
      * @brief Whether software rendering should be forced.
diff --git a/x11info.h b/x11info.h
index f1e68dc..d1da067 100644
--- a/x11info.h
+++ b/x11info.h
@@ -6,7 +6,9 @@
 
 #pragma once
 
+#ifdef HAVE_X11
 #include <X11/Xlib.h>
+#endif
 
 #include <QGuiApplication>
 #include <QString>
@@ -17,9 +19,14 @@ namespace X11Info
 {
 [[nodiscard]] inline bool isPlatformX11()
 {
+#ifdef HAVE_X11
     return QGuiApplication::platformName() == "xcb"_L1;
+#else
+    return false;
+#endif
 }
 
+#ifdef HAVE_X11
 [[nodiscard]] inline auto display()
 {
     return qGuiApp->nativeInterface<QNativeInterface::QX11Application>()->display();
@@ -34,4 +41,5 @@ namespace X11Info
 {
     return DefaultRootWindow(display());
 }
+#endif // HAVE_X11
 }
